<%= @doctor.name %>
<%= @doctor.department %>
<% @doctor.appointments.each do |appointment| %>
  <p><%= appointment.appointment_datetime.strftime('%B %d, %Y at %H:%M') %>, <%= link_to(appointment.patient.name, appointment.patient) %></p>
<% end %>
